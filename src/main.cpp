#include <iostream>
#include <string>
#include <vector>
#include "MSGlang.h"
int main() {
    let m = object[values 0 , 1 , 2, 3];

    let m1 = object[values "2", 3];

    cout << input("enter a number:");

    m1.print();
    return 0;
}

    int k = s.size();
    for(int i = 0; i < k; i++){
        if(s[i] == '.'){
            throw peos
        }
    }